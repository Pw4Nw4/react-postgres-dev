import React from 'react';

function App() {
  return (
    <div style={{ padding: '1rem' }}>
      <h1>Hello, React + Docker!</h1>
      <p>Edit these files via File Browser (port 8080) and watch hot-reload on port 3000.</p>
    </div>
  );
}

export default App;
